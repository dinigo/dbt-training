version: 2

seeds:
  - name: raw_payments
    description: Raw list of payments cause it was not available from BigQuery
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: order_id
      - name: payment_method
      - name: amount

